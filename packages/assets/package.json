{
  "name": "@proffy/assets",
  "version": "0.1.0",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "pre:build": "svgr --icon -d dist src --title-prop",
    "post:build": "node ./generate-compiled-exports",
    "transpile": "yarn pre:build && yarn post:build"
  },
  "dependencies": {
    "@svgr/cli": "^5.4.0"
  },
  "files": [
    "dist"
  ]
}
